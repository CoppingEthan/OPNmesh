# Normalise line endings in the repository; files that Linux hosts execute
# or compare byte-for-byte must check out as LF on every platform.
* text=auto
*.sh text eol=lf
deploy/** text eol=lf
sim/entrypoints/** text eol=lf
test/golden/** text eol=lf
