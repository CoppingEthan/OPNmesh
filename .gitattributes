# Every text file is stored and checked out with LF on every platform: the
# tree is executed on Linux (installers, sim entrypoints, nftables files),
# built into Docker images straight from the working copy, and compared
# byte-for-byte by the golden tests. Binary files are detected automatically.
* text=auto eol=lf
